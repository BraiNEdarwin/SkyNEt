# Config file for the experiments

# Software reservoir settings
nodes = [1, 50, 1]  # [input, reservoir, output]
inputscaling = 0.2
spectralradius = 0.99
weightdensity = 1  # 1 is fully connected matrix
trainwith = 'ridgeregression'  # or pseudoinverse
rralpha = 1e-3
skipstates = 200

# Benchmarks settings
benchmark = 'wr'  #'wr' for waveform regression benchmark
WavePeriods = 15
WaveFrequency = 1

# I/O settings
SampleFreq = 100

# Evolution settings
genes = 4
genomes = 25
generations = 200
generange = [[19, 20], [0, 50], [0, 10], [0, 1]]
genelabels = ['Nodes','Input scaling','Spectral radius','Weight density']
fitnessAvg = 1  #amount of runs per genome

# Save settings
filepath = 'C:\\Users\\bram\\Documents\\uni\\master_thesis\\SkyNEt\\doubleSine'